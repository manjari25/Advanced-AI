1).To run, type matlab in the linux terminal and run the HW4.m file.
2).Program will pause after each image is processed. Hit any key to continue.
3).For Part I and Part V, I tried different values of sigma, filter length and threshold. I've written code for generating a gaussian with each of these combinations and different thresholds. Swap comments of these to see output at different values.
4).The code generates and saves all images in the Output folder.

-Manjari